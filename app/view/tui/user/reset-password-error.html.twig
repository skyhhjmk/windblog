{% extends "base.html.twig" %}

{% block title %}Error - {{ page_title|default('WindBlog') }}{% endblock %}

{% block content %}
    <div class="min-h-[50vh] flex items-center justify-center py-16">
        <div class="max-w-lg w-full tui-box" data-title="ERROR">
            <div class="text-center font-mono">
                <div class="mb-4">
                    <span class="text-4xl text-red-500">⚠</span>
                </div>
                <h2 class="text-2xl font-bold text-red-500 mb-2">Link Invalid</h2>
                <p class="text-gray-500 mb-6">{{ message|default('Reset link invalid or expired') }}</p>
                <div>
                    <a href="/user/forgot-password" class="text-blue-500 hover:text-blue-400 hover:underline">
                        [Request New Reset Link]
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
